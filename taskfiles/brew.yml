version: '3'

tasks:
  bundle:
    cmds:
      - brew bundle --cleanup --cask --tap --no-lock --zap

  dump:
    cmds:
      - brew bundle dump --force --cask --tap --no-lock

  install-cask:
    cmds:
      - brew install --cask --appdir=~/Applications {{.CLI_ARGS}}
