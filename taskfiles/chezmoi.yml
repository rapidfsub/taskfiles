version: '3'

tasks:
  apply:
    deps:
      - init-config
    cmds:
      - chezmoi apply

  init-config:
    cmds:
      - mkdir -p ~/.config
      - cp -rf .home/.config/chezmoi ~/.config/chezmoi
    status:
      - test -d ~/.config/chezmoi
